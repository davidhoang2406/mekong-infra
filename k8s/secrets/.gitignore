# Never commit real secrets. Template files with <base64-...> placeholders are OK.
# Files with actual base64-encoded values must stay local.
minio-credentials.yaml
dagster-postgres.yaml
telegram-credentials.yaml
jupyter-credentials.yaml
